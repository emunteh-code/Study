---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
const classes = ["reading-container", className].filter(Boolean).join(" ");
---

<div class={classes}>
  <slot />
</div>
