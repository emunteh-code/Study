---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
const classes = ["content-container", className].filter(Boolean).join(" ");
---

<div class={classes}>
  <slot />
</div>
